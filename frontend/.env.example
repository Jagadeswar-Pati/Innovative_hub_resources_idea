VITE_APP_URL=https://inovative-hub.com
# Backend API base URL (required for contact form and API on live site). No trailing slash.
# Local dev: leave empty to use Vite proxy. Production (Vercel): set to your Render backend URL.
VITE_API_URL=
VITE_GOOGLE_CLIENT_ID=
VITE_RAZORPAY_KEY_ID=
